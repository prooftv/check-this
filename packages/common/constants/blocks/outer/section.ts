export enum SECTION_ALIGNMENT_OPTION {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right',
}

export enum SECTION_IMAGE_PLACEMENT {
  LEFT = 'left',
  RIGHT = 'right',
}

export enum SECTION_MEDIA_TYPE {
  NONE = 'NONE',
  IMAGE = 'IMAGE',
  VIDEO = 'VIDEO',
}

export enum SECTION_MEDIA_SIZE {
  SMALL = 'small',
  LARGE = 'large',
}
